version: '3.9'
services:
  content-writing-agent:
    build: .
    ports:
      - '5000:5000'
    environment:
      # Add environment variables here if any needed
      - FLASK_ENV=production
    restart: unless-stopped
