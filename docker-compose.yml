version: "3.9"
services:
  contentwritingagent:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
    volumes:
      - .:/app
    command: gunicorn -b 0.0.0.0:5000 app:app
