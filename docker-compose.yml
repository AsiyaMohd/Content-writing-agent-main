version: '3.8'

services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - OPENAI_API_KEY
      - TAVILY_API_KEY
    volumes:
      - .:/app
    command: flask run --host=0.0.0.0
