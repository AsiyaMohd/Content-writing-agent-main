version: '3.9'

services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - TAVILY_API_KEY
      - OPENAI_API_KEY
      - GOOGLE_API_KEY
    volumes:
      - .:/app
    restart: unless-stopped
