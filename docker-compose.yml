version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      # Add here environment variables like OPENAI_API_KEY if used
    volumes:
      - .:/app
    command: gunicorn -b 0.0.0.0:5000 app:app
