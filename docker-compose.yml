version: '3.8'

services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      - TAVILY_API_KEY=${TAVILY_API_KEY}
    volumes:
      - .:/app
    command: gunicorn --bind 0.0.0.0:5000 app:app
