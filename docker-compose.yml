version: "3.8"
services:
  content-agent:
    build: .
    ports:
      - "5000:5000"
    environment:
      - OPENAI_API_KEY
      - GOOGLE_API_KEY
    volumes:
      - .:/app
    restart: unless-stopped
